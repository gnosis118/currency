// Stackbit configuration disabled - using Netlify CMS instead
// This file is kept for compatibility but the visual editor is handled by /admin/

export default {
  // Minimal config to prevent errors
  stackbitVersion: '~0.6.0',
  ssgName: 'custom'
};

